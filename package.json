{
  "name": "ultraplus-free",
  "version": "1.0.0-phase1",
  "private": true,
  "description": "Self-hosted Cloudflare Worker management panel — modular, secure, Persian-first",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "node scripts/build.js",
    "lint": "node scripts/verify.js --lint",
    "test": "node --test tests/unit/*.test.js",
    "verify": "node scripts/verify.js",
    "migrate:local": "wrangler d1 migrations apply ultraplus-db --local"
  },
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
