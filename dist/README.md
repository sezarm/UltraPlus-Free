# Worker build parts

If `worker.js` at repo root is incomplete, join in order:

```
cat dist/worker.p0.js dist/worker.p1.js dist/worker.p2.js > worker.js
```

Then deploy `worker.js` to Cloudflare.

Or download the full single file from the project release / chat artifact (v5.1.0).
