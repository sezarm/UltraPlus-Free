# API

## Public
- GET /healthz
- GET /sub/:token (?format=base64|raw|clash|singbox)
- GET|POST /dns-query
- GET /ip
- GET /tools/latency?host=
- POST /telegram/webhook
- WebSocket upgrade → proxy

## Admin (cookie)
- /api/users · /api/stats · /api/diagnostics · /api/update-check
- /api/hosts · /api/radar · /api/plugins · /api/health-hosts
