name: Update check metadata
on:
  schedule:
    - cron: "0 6 * * 1"
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: cat VERSION version.json
